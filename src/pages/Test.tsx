import { RouteComponentProps } from '@reach/router';
import * as React from 'react';
import { MainAppImage } from '../components/headericon/MainAppImage';

const Test = (_props: RouteComponentProps) => (
  <main>
    <MainAppImage />
  </main>
);

export { Test };
